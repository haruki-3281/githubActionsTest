name: Action Sample

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  job_id:
    runs-on: ubuntu-latest
    steps:
      - uses: 'actions/checkout@v3'

      - name: docker test
        run: docker
